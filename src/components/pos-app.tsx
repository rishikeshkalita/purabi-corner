'use client';

import { useMemo, useState } from 'react';
import { BarChart3, Clock3, LayoutGrid, Menu, Minus, Plus, Receipt, Settings2, Share2, ShoppingBag, Trash2, X } from 'lucide-react';
import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { demoProducts, demoSales } from '@/lib/demo-data';
import type { Product, Sale } from '@/lib/types';
import { BUSINESS_DAY_RESET_HOUR, businessDayBounds, businessDayKey, formatBusinessDay, isInBusinessDay } from '@/lib/business-day';

type Tab = 'counter' | 'history' | 'analytics' | 'menu';
type AnalyticsMode = 'daily' | 'monthly' | 'custom' | 'yearly';
const money = (n: number) => `₹${n.toLocaleString('en-IN', { minimumFractionDigits: 0, maximumFractionDigits: 0 })}`;
const localMonthKey = (key: string) => key.slice(0, 7);
const localYearKey = (key: string) => key.slice(0, 4);

function rangeForMode(mode: AnalyticsMode, dailyKey: string, monthKey: string, yearKey: string, customStart: string, customEnd: string) {
  if (mode === 'daily') return businessDayBounds(dailyKey);
  if (mode === 'monthly') {
    const [year, month] = monthKey.split('-').map(Number);
    const startKey = `${year}-${String(month).padStart(2, '0')}-01`;
    const next = new Date(year, month, 1);
    const endKey = `${next.getFullYear()}-${String(next.getMonth() + 1).padStart(2, '0')}-01`;
    return { start: businessDayBounds(startKey).start, end: businessDayBounds(endKey).start };
  }
  if (mode === 'yearly') {
    const year = Number(yearKey);
    return { start: businessDayBounds(`${year}-01-01`).start, end: businessDayBounds(`${year + 1}-01-01`).start };
  }
  const start = customStart || dailyKey;
  const end = customEnd || start;
  const normalizedStart = start <= end ? start : end;
  const normalizedEnd = start <= end ? end : start;
  return { start: businessDayBounds(normalizedStart).start, end: businessDayBounds(normalizedEnd).end };
}

function pdfEscape(value: string) { return value.replace(/\\/g, '\\\\').replace(/\(/g, '\\(').replace(/\)/g, '\\)'); }
function pdfRgb(r: number, g: number, b: number, stroke = false) { return `${(r / 255).toFixed(3)} ${(g / 255).toFixed(3)} ${(b / 255).toFixed(3)} ${stroke ? 'RG' : 'rg'}`; }
function pdfText(text: string, x: number, y: number, size: number, font = 'F1', color = [20, 30, 45]) { return `${pdfRgb(color[0], color[1], color[2])}\nBT /${font} ${size} Tf ${x} ${y} Td (${pdfEscape(text)}) Tj ET`; }
function pdfRect(x: number, y: number, w: number, h: number, fill: number[], radius = 0) {
  if (!radius) return `${pdfRgb(fill[0], fill[1], fill[2])}\n${x} ${y} ${w} ${h} re f`;
  const k = 0.5522848; const r = Math.min(radius, w / 2, h / 2); const c = r * k;
  return `${pdfRgb(fill[0], fill[1], fill[2])}\n${x + r} ${y} m ${x + w - r} ${y} l ${x + w - r + c} ${y} ${x + w} ${y + r - c} ${x + w} ${y + r} c ${x + w} ${y + h - r} l ${x + w} ${y + h - r + c} ${x + w - r + c} ${y + h} ${x + w - r} ${y + h} c ${x + r} ${y + h} l ${x + r - c} ${y + h} ${x} ${y + h - r + c} ${x} ${y + h - r} c ${x} ${y + r} l ${x} ${y + r - c} ${x + r - c} ${y} ${x + r} ${y} c f`;
}
function pdfLine(x1: number, y1: number, x2: number, y2: number, color = [220, 226, 235], width = 1) { return `${pdfRgb(color[0], color[1], color[2])}\n${width} w ${x1} ${y1} m ${x2} ${y2} l S`; }
function shortDate(date: Date) { return date.toLocaleDateString('en-IN', { day: '2-digit', month: 'short', year: 'numeric' }); }

function makeAnalyticsPdf({ modeLabel, periodLabel, revenue, profit, transactions, peak, periods, top }: { modeLabel: string; periodLabel: string; revenue: number; profit: number; transactions: number; peak: { label: string; time: string; revenue: number }; periods: { label: string; time: string; revenue: number; transactions: number; profit: number }[]; top: { name: string; count: number; profit: number }[] }) {
  const ops: string[] = []; const encoder = new TextEncoder();
  const navy = [7, 16, 29], ink = [24, 35, 50], muted = [101, 116, 139], line = [224, 230, 237], sky = [14, 165, 233], pale = [239, 248, 253];
  ops.push(pdfRect(0, 0, 595, 842, [248, 250, 252]));
  ops.push(pdfRect(0, 700, 595, 142, navy)); ops.push(pdfRect(0, 700, 595, 5, sky));
  ops.push(pdfText('PURABI CORNER', 42, 794, 12, 'F2', [125, 211, 252]));
  ops.push(pdfText('Analytics Report', 42, 766, 25, 'F2', [255, 255, 255]));
  ops.push(pdfText(`${modeLabel.toUpperCase()} PERFORMANCE`, 42, 744, 9, 'F2', [186, 200, 218]));
  ops.push(pdfRect(398, 754, 155, 35, [20, 36, 55], 10));
  ops.push(pdfText('REPORT PERIOD', 411, 775, 7, 'F2', [125, 211, 252])); ops.push(pdfText(periodLabel, 411, 761, 9, 'F1', [255, 255, 255]));
  const cards = [{ x: 42, label: 'TOTAL REVENUE', value: `INR ${revenue.toLocaleString('en-IN')}` }, { x: 225, label: 'TOTAL PROFIT', value: `INR ${profit.toLocaleString('en-IN')}` }, { x: 408, label: 'TRANSACTIONS', value: transactions.toLocaleString('en-IN') }];
  cards.forEach((card) => { ops.push(pdfRect(card.x, 636, 145, 76, [255, 255, 255], 10)); ops.push(pdfText(card.label, card.x + 13, 690, 7, 'F2', muted)); ops.push(pdfText(card.value, card.x + 13, 661, 17, 'F2', ink)); });
  ops.push(pdfRect(42, 566, 511, 54, pale, 10)); ops.push(pdfText('PEAK SALES PERIOD', 57, 600, 7, 'F2', [2, 132, 199])); ops.push(pdfText(`${peak.label}  |  ${peak.time}`, 57, 581, 13, 'F2', ink)); ops.push(pdfText(`Revenue INR ${peak.revenue.toLocaleString('en-IN')}`, 390, 583, 10, 'F2', [2, 132, 199]));
  ops.push(pdfText('SALES BY TIME PERIOD', 42, 535, 10, 'F2', ink));
  ops.push(pdfRect(42, 503, 511, 22, [230, 238, 245], 5));
  ops.push(pdfText('PERIOD', 54, 511, 7, 'F2', muted)); ops.push(pdfText('TIME', 180, 511, 7, 'F2', muted)); ops.push(pdfText('REVENUE', 305, 511, 7, 'F2', muted)); ops.push(pdfText('TX', 410, 511, 7, 'F2', muted)); ops.push(pdfText('PROFIT', 470, 511, 7, 'F2', muted));
  periods.forEach((p, i) => { const y = 480 - i * 25; if (i % 2 === 0) ops.push(pdfRect(42, y - 7, 511, 23, [255, 255, 255])); ops.push(pdfText(p.label, 54, y, 8, 'F2', ink)); ops.push(pdfText(p.time, 180, y, 8, 'F1', muted)); ops.push(pdfText(`INR ${p.revenue.toLocaleString('en-IN')}`, 305, y, 8, 'F1', ink)); ops.push(pdfText(String(p.transactions), 410, y, 8, 'F1', ink)); ops.push(pdfText(`INR ${p.profit.toLocaleString('en-IN')}`, 470, y, 8, 'F1', ink)); });
  const topY = 320;
  ops.push(pdfText('TOP SELLING ITEMS', 42, topY, 10, 'F2', ink)); ops.push(pdfRect(42, topY - 32, 511, 22, [230, 238, 245], 5));
  ops.push(pdfText('#', 54, topY - 24, 7, 'F2', muted)); ops.push(pdfText('PRODUCT', 80, topY - 24, 7, 'F2', muted)); ops.push(pdfText('UNITS', 360, topY - 24, 7, 'F2', muted)); ops.push(pdfText('PROFIT', 450, topY - 24, 7, 'F2', muted));
  top.slice(0, 7).forEach((p, i) => { const y = topY - 57 - i * 24; if (i % 2 === 0) ops.push(pdfRect(42, y - 7, 511, 22, [255, 255, 255])); const name = p.name.length > 38 ? `${p.name.slice(0, 35)}...` : p.name; ops.push(pdfText(String(i + 1), 54, y, 8, 'F2', sky)); ops.push(pdfText(name, 80, y, 8, 'F1', ink)); ops.push(pdfText(String(p.count), 360, y, 8, 'F1', ink)); ops.push(pdfText(`INR ${p.profit.toLocaleString('en-IN')}`, 450, y, 8, 'F1', ink)); });
  if (!top.length) ops.push(pdfText('No product sales in this period.', 80, topY - 57, 8, 'F1', muted));
  ops.push(pdfLine(42, 62, 553, 62, line)); ops.push(pdfText('Purabi Corner  |  Business day resets at 03:00 AM', 42, 45, 7, 'F1', muted)); ops.push(pdfText(`Generated ${new Date().toLocaleString('en-IN')}`, 375, 45, 7, 'F1', muted));
  const content = ops.join('\n');
  const objects = ['<< /Type /Catalog /Pages 2 0 R >>', '<< /Type /Pages /Kids [3 0 R] /Count 1 >>', '<< /Type /Page /Parent 2 0 R /MediaBox [0 0 595 842] /Resources << /Font << /F1 4 0 R /F2 5 0 R >> >> /Contents 6 0 R >>', '<< /Type /Font /Subtype /Type1 /BaseFont /Helvetica >>', '<< /Type /Font /Subtype /Type1 /BaseFont /Helvetica-Bold >>', `<< /Length ${encoder.encode(content).length} >>\nstream\n${content}\nendstream`];
  let pdf = '%PDF-1.4\n'; const offsets = [0]; objects.forEach((object, index) => { offsets[index + 1] = encoder.encode(pdf).length; pdf += `${index + 1} 0 obj\n${object}\nendobj\n`; }); const xref = encoder.encode(pdf).length; pdf += `xref\n0 ${objects.length + 1}\n0000000000 65535 f \n${offsets.slice(1).map((offset) => `${String(offset).padStart(10, '0')} 00000 n `).join('\n')}\ntrailer\n<< /Size ${objects.length + 1} /Root 1 0 R >>\nstartxref\n${xref}\n%%EOF`;
  return new Blob([pdf], { type: 'application/pdf' });
}

export default function PosApp() {
  const [tab, setTab] = useState<Tab>('counter'); const [products, setProducts] = useState<Product[]>(demoProducts); const [sales, setSales] = useState<Sale[]>(demoSales); const [customOpen, setCustomOpen] = useState(false); const [customPrice, setCustomPrice] = useState('');
  const currentBusinessDay = businessDayKey(); const [historyDate, setHistoryDate] = useState(currentBusinessDay); const [analyticsMode, setAnalyticsMode] = useState<AnalyticsMode>('daily'); const [analyticsDailyDate, setAnalyticsDailyDate] = useState(currentBusinessDay); const [analyticsMonth, setAnalyticsMonth] = useState(localMonthKey(currentBusinessDay)); const [analyticsYear, setAnalyticsYear] = useState(localYearKey(currentBusinessDay)); const [analyticsCustomStart, setAnalyticsCustomStart] = useState(currentBusinessDay); const [analyticsCustomEnd, setAnalyticsCustomEnd] = useState(currentBusinessDay); const [editing, setEditing] = useState<Product | null>(null);
  const todayKey = currentBusinessDay; const todaySales = sales.filter((s) => isInBusinessDay(s.timestamp, todayKey)); const liveTotal = useMemo(() => todaySales.reduce((a, b) => a + b.price_charged, 0), [todaySales]); const history = sales.filter((s) => isInBusinessDay(s.timestamp, historyDate)).sort((a, b) => b.timestamp.localeCompare(a.timestamp)); const analyticsRange = rangeForMode(analyticsMode, analyticsDailyDate, analyticsMonth, analyticsYear, analyticsCustomStart, analyticsCustomEnd); const analyticsSales = sales.filter((s) => { const time = new Date(s.timestamp).getTime(); return time >= analyticsRange.start.getTime() && time < analyticsRange.end.getTime(); }); const revenue = analyticsSales.reduce((a, b) => a + b.price_charged, 0); const profit = analyticsSales.reduce((a, b) => a + b.profit_recorded, 0); const dailyCounts = useMemo(() => Object.fromEntries(products.map((p) => [p.id, todaySales.filter((s) => s.product_id === p.id).length])), [products, todaySales]); const top = products.map((p) => ({ name: p.name, count: analyticsSales.filter((s) => s.product_id === p.id).length, profit: analyticsSales.filter((s) => s.product_id === p.id).reduce((a, b) => a + b.profit_recorded, 0) })).sort((a, b) => b.count - a.count).slice(0, 6);
  const addSale = (p: Product) => setSales((s) => [{ id: crypto.randomUUID(), product_id: p.id, product_name: p.name, timestamp: new Date().toISOString(), price_charged: p.price, profit_recorded: p.profit_margin }, ...s]); const removeSale = (p: Product) => { const victim = sales.filter((s) => s.product_id === p.id && isInBusinessDay(s.timestamp, todayKey)).sort((a, b) => b.timestamp.localeCompare(a.timestamp))[0]; if (victim) setSales((s) => s.filter((x) => x.id !== victim.id)); }; const addCustom = () => { const price = Number(customPrice); if (!price || price < 0) return; setSales((s) => [{ id: crypto.randomUUID(), product_id: null, product_name: 'Custom Item', timestamp: new Date().toISOString(), price_charged: price, profit_recorded: 0 }, ...s]); setCustomPrice(''); setCustomOpen(false); };
  return <main className="min-h-screen bg-[radial-gradient(circle_at_top,rgba(19,78,123,.22),transparent_35%),#050914] pb-24"><header className="sticky top-0 z-30 border-b border-white/5 bg-[#050914]/85 backdrop-blur-xl"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-4 md:px-6"><div><p className="text-xs uppercase tracking-[.28em] text-sky-400">Purabi Corner</p><h1 className="text-xl font-semibold">Dairy POS Console</h1></div><div className="hidden items-center gap-2 rounded-full border border-white/10 bg-white/[.03] px-4 py-2 text-sm text-slate-300 md:flex"><Receipt size={16}/> Live shift · {todaySales.length} bills</div></div></header><div className="mx-auto max-w-7xl px-4 pt-4 md:px-6">{tab === 'counter' && <Counter products={products.map((p) => ({ ...p, daily_count: dailyCounts[p.id] ?? 0 }))} liveTotal={liveTotal} addSale={addSale} removeSale={removeSale} onCustom={() => setCustomOpen(true)} businessDay={todayKey} />}{tab === 'history' && <History date={historyDate} setDate={setHistoryDate} rows={history} />}{tab === 'analytics' && <Analytics mode={analyticsMode} setMode={setAnalyticsMode} dailyDate={analyticsDailyDate} setDailyDate={setAnalyticsDailyDate} month={analyticsMonth} setMonth={setAnalyticsMonth} year={analyticsYear} setYear={setAnalyticsYear} customStart={analyticsCustomStart} setCustomStart={setAnalyticsCustomStart} customEnd={analyticsCustomEnd} setCustomEnd={setAnalyticsCustomEnd} range={analyticsRange} sales={analyticsSales} revenue={revenue} profit={profit} top={top} />}{tab === 'menu' && <MenuManager products={products} setProducts={setProducts} editing={editing} setEditing={setEditing} />}</div><nav className="fixed bottom-0 left-0 right-0 z-40 border-t border-white/10 bg-[#07101d]/95 backdrop-blur-xl"><div className="mx-auto grid max-w-3xl grid-cols-4">{([['counter', 'Counter', LayoutGrid], ['history', 'History', Clock3], ['analytics', 'Analytics', BarChart3], ['menu', 'Menu', Menu]] as const).map(([k, label, Icon]) => <button key={k} onClick={() => setTab(k)} className={`flex flex-col items-center gap-1 px-2 py-3 text-xs ${tab === k ? 'text-sky-300' : 'text-slate-500'}`}><Icon size={19}/><span>{label}</span></button>)}</div></nav>{customOpen && <div className="fixed inset-0 z-50 flex items-end justify-center bg-black/60 p-4 md:items-center"><div className="glass w-full max-w-md rounded-3xl p-5 shadow-2xl"><div className="mb-4 flex items-center justify-between"><h2 className="font-semibold">Custom item</h2><button onClick={() => setCustomOpen(false)}><X size={18}/></button></div><label className="text-xs text-slate-400">Price charged</label><input value={customPrice} onChange={(e) => setCustomPrice(e.target.value)} inputMode="decimal" placeholder="₹0" className="mt-2 w-full rounded-2xl border border-white/10 bg-black/20 px-4 py-3 outline-none focus:border-sky-400"/><button onClick={addCustom} className="mt-4 w-full rounded-2xl bg-sky-400 py-3 font-semibold text-slate-950">Add to sale</button></div></div>}</main>;
}

function Counter({ products, liveTotal, addSale, removeSale, onCustom, businessDay }: { products: Product[]; liveTotal: number; addSale: (p: Product) => void; removeSale: (p: Product) => void; onCustom: () => void; businessDay: string }) { return <section><div className="glass glow mb-5 rounded-3xl p-5 md:p-6"><div className="mb-3 flex items-center justify-between gap-3"><div><p className="text-[10px] uppercase tracking-[.2em] text-sky-400">Business Day</p><p className="text-sm font-medium">{formatBusinessDay(businessDay)}</p></div><span className="rounded-full border border-white/10 bg-white/[.03] px-3 py-1 text-[10px] text-slate-400">Resets daily · {BUSINESS_DAY_RESET_HOUR}:00 AM</span></div><div className="flex items-start justify-between"><div><p className="text-xs uppercase tracking-widest text-slate-400">Live Daily Sales Total</p><p className="mt-2 text-4xl font-semibold tracking-tight">{money(liveTotal)}</p></div><div className="rounded-2xl border border-sky-400/20 bg-sky-400/10 p-3 text-sky-300"><ShoppingBag/></div></div></div><div className="grid grid-cols-2 gap-3 md:grid-cols-3 lg:grid-cols-4">{products.map((p) => <div key={p.id} className="glass overflow-hidden rounded-3xl p-3"><div className="aspect-[1.35] overflow-hidden rounded-2xl bg-slate-900">{p.image_url ? <img src={p.image_url} alt="" className="h-full w-full object-cover"/> : <div className="grid h-full place-items-center text-slate-600">No image</div>}</div><div className="pt-3"><div className="flex items-start justify-between gap-2"><div><p className="font-medium leading-tight">{p.name}</p><p className="mt-1 text-sm text-slate-400">{money(p.price)}</p></div><span className="rounded-full bg-sky-400/10 px-2 py-1 text-[10px] font-semibold text-sky-300">{p.daily_count} sold</span></div><div className="mt-3 flex gap-2"><button onClick={() => removeSale(p)} disabled={p.daily_count === 0} aria-label={`Remove latest ${p.name} sale`} className="grid h-11 w-11 place-items-center rounded-xl border border-white/10 bg-white/[.03] text-slate-300 disabled:cursor-not-allowed disabled:opacity-30"><Minus size={18}/></button><button onClick={() => addSale(p)} className="flex-1 rounded-xl bg-sky-400 font-semibold text-slate-950"><span className="inline-flex items-center gap-1"><Plus size={18}/> Sell</span></button></div></div></div>)}</div><button onClick={onCustom} className="fixed bottom-24 right-5 rounded-2xl border border-sky-400/30 bg-sky-400 px-4 py-3 font-semibold text-slate-950 shadow-[0_0_28px_rgba(56,189,248,.25)]">+ Custom</button></section>; }

function History({ date, setDate, rows }: { date: string; setDate: (x: string) => void; rows: Sale[] }) { return <section><div className="mb-5 flex flex-col gap-3 md:flex-row md:items-end md:justify-between"><div><p className="text-xs uppercase tracking-widest text-slate-500">Business-day transaction feed</p><h2 className="mt-1 text-2xl font-semibold">History</h2></div><input type="date" value={date} onChange={(e) => setDate(e.target.value)} className="rounded-2xl border border-white/10 bg-white/[.04] px-4 py-3 text-sm"/></div><div className="space-y-2">{rows.map((s, i) => <div key={s.id} className="glass flex items-center justify-between rounded-2xl px-4 py-3"><div className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-xl bg-sky-400/10 text-sky-300">{i + 1}</div><div><p className="font-medium">{s.product_name}</p><p className="text-xs text-slate-500">{new Date(s.timestamp).toLocaleTimeString('en-IN', { hour: '2-digit', minute: '2-digit' })}</p></div></div><div className="text-right"><p className="font-semibold">{money(s.price_charged)}</p><p className="text-xs text-slate-500">Profit {money(s.profit_recorded)}</p></div></div>)}{!rows.length && <div className="glass rounded-3xl p-10 text-center text-slate-500">No sales on this date.</div>}</div></section>; }

function Analytics({ mode, setMode, dailyDate, setDailyDate, month, setMonth, year, setYear, customStart, setCustomStart, customEnd, setCustomEnd, range, sales, revenue, profit, top }: { mode: AnalyticsMode; setMode: (x: AnalyticsMode) => void; dailyDate: string; setDailyDate: (x: string) => void; month: string; setMonth: (x: string) => void; year: string; setYear: (x: string) => void; customStart: string; setCustomStart: (x: string) => void; customEnd: string; setCustomEnd: (x: string) => void; range: { start: Date; end: Date }; sales: Sale[]; revenue: number; profit: number; top: { name: string; count: number; profit: number }[] }) {
  const rangeDays = Math.max(1, Math.round((range.end.getTime() - range.start.getTime()) / 86400000));
  const trend = useMemo(() => { const buckets = new Map<string, { label: string; revenue: number; profit: number }>(); const add = (key: string, label: string, sale: Sale) => { const current = buckets.get(key) ?? { label, revenue: 0, profit: 0 }; current.revenue += sale.price_charged; current.profit += sale.profit_recorded; buckets.set(key, current); }; sales.forEach((sale) => { const day = businessDayKey(new Date(sale.timestamp)); if (mode === 'yearly') { const key = localMonthKey(day); const date = new Date(`${key}-01T12:00:00`); add(key, date.toLocaleDateString('en-IN', { month: 'short' }), sale); } else add(day, new Date(`${day}T12:00:00`).toLocaleDateString('en-IN', { day: '2-digit', month: 'short' }), sale); }); return [...buckets.entries()].sort(([a], [b]) => a.localeCompare(b)).map(([, value]) => value); }, [mode, sales]);
  const periodStats = useMemo(() => { const periods = [{ key: 'early', label: 'Early morning', time: '3 AM - 7 AM', start: 3, end: 7 }, { key: 'morning', label: 'Morning', time: '7 AM - 11 AM', start: 7, end: 11 }, { key: 'afternoon', label: 'Afternoon', time: '11 AM - 3 PM', start: 11, end: 15 }, { key: 'evening', label: 'Evening', time: '3 PM - 7 PM', start: 15, end: 19 }, { key: 'night', label: 'Night', time: '7 PM - 11 PM', start: 19, end: 23 }]; return periods.map((period) => { const matched = sales.filter((sale) => { const d = new Date(sale.timestamp); const hour = d.getHours(); const businessHour = hour < 3 ? hour + 24 : hour; return businessHour >= period.start && businessHour < period.end; }); return { ...period, revenue: matched.reduce((sum, sale) => sum + sale.price_charged, 0), transactions: matched.length, profit: matched.reduce((sum, sale) => sum + sale.profit_recorded, 0) }; }); }, [sales]);
  const peakPeriod = periodStats.reduce((best, item) => item.revenue > best.revenue ? item : best, periodStats[0]); const modeLabel = mode === 'daily' ? 'Daily' : mode === 'monthly' ? 'Monthly' : mode === 'yearly' ? 'Yearly' : 'Custom range'; const periodLabel = mode === 'daily' ? formatBusinessDay(businessDayKey(new Date(`${dailyDate}T12:00:00`))) : `${shortDate(range.start)} - ${shortDate(range.end)}`;
  const sharePdf = async () => { const blob = makeAnalyticsPdf({ modeLabel, periodLabel, revenue, profit, transactions: sales.length, peak: peakPeriod, periods: periodStats, top }); const file = new File([blob], `Purabi-Corner-Analytics-${modeLabel.replace(/\s+/g, '-')}.pdf`, { type: 'application/pdf' }); try { if (navigator.share && (!navigator.canShare || navigator.canShare({ files: [file] }))) await navigator.share({ title: 'Purabi Corner Analytics', text: `Purabi Corner ${modeLabel} analytics`, files: [file] }); else { const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = file.name; a.click(); URL.revokeObjectURL(url); } } catch (error) { if ((error as DOMException).name !== 'AbortError') { const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = file.name; a.click(); URL.revokeObjectURL(url); } } };
  return <section><div className="mb-5"><div className="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><p className="text-xs uppercase tracking-widest text-slate-500">Business-day performance dashboard</p><h2 className="mt-1 text-2xl font-semibold">Analytics</h2></div><div className="grid grid-cols-4 gap-1 rounded-2xl border border-white/10 bg-white/[.03] p-1">{(['daily', 'monthly', 'custom', 'yearly'] as AnalyticsMode[]).map((item) => <button key={item} onClick={() => setMode(item)} className={`rounded-xl px-3 py-2 text-xs font-semibold transition ${mode === item ? 'bg-sky-400 text-slate-950' : 'text-slate-400 hover:text-white'}`}>{item[0].toUpperCase() + item.slice(1)}</button>)}</div></div><div className="mt-4 rounded-3xl border border-white/10 bg-white/[.025] p-4">{mode === 'daily' && <div className="flex flex-col gap-2"><label className="text-[10px] uppercase tracking-widest text-slate-500">Business day</label><input type="date" value={dailyDate} onChange={(e) => setDailyDate(e.target.value)} className="w-full min-w-0 max-w-full rounded-2xl border border-white/10 bg-white/[.04] px-4 py-3 text-sm"/></div>}{mode === 'monthly' && <div className="flex flex-col gap-2"><label className="text-[10px] uppercase tracking-widest text-slate-500">Select month</label><input type="month" value={month} onChange={(e) => setMonth(e.target.value)} className="w-full min-w-0 max-w-full rounded-2xl border border-white/10 bg-white/[.04] px-4 py-3 text-sm"/><p className="text-xs text-slate-500">Includes every business day from 03:00 AM on the 1st through 02:59 AM on the next month.</p></div>}{mode === 'yearly' && <div className="flex flex-col gap-2"><label className="text-[10px] uppercase tracking-widest text-slate-500">Select year</label><select value={year} onChange={(e) => setYear(e.target.value)} className="w-full min-w-0 max-w-full rounded-2xl border border-white/10 bg-white/[.04] px-4 py-3 text-sm"><option value={String(Number(year) - 2)}>{Number(year) - 2}</option><option value={String(Number(year) - 1)}>{Number(year) - 1}</option><option value={year}>{year}</option><option value={String(Number(year) + 1)}>{Number(year) + 1}</option><option value={String(Number(year) + 2)}>{Number(year) + 2}</option></select></div>}{mode === 'custom' && <div><div className="grid gap-3 md:grid-cols-2"><div className="flex min-w-0 flex-col gap-2"><label className="text-[10px] uppercase tracking-widest text-slate-500">From</label><input type="date" value={customStart} onChange={(e) => setCustomStart(e.target.value)} className="w-full min-w-0 max-w-full rounded-2xl border border-white/10 bg-white/[.04] px-4 py-3 text-sm"/></div><div className="flex min-w-0 flex-col gap-2"><label className="text-[10px] uppercase tracking-widest text-slate-500">To</label><input type="date" value={customEnd} onChange={(e) => setCustomEnd(e.target.value)} className="w-full min-w-0 max-w-full rounded-2xl border border-white/10 bg-white/[.04] px-4 py-3 text-sm"/></div></div><p className="mt-2 text-xs text-slate-500">Custom reports use the same 03:00 AM business-day boundary.</p></div>}</div></div><div className="grid gap-3 sm:grid-cols-3"><Metric label="Total Revenue" value={money(revenue)}/><Metric label="Total Profit" value={money(profit)}/><Metric label="Transactions" value={sales.length.toLocaleString('en-IN')}/></div><div className="mt-4 glass rounded-3xl p-5"><div className="flex flex-col gap-4 md:flex-row md:items-center md:justify-between"><div><div className="flex items-center gap-2"><Clock3 size={18} className="text-sky-300"/><h3 className="font-semibold">Peak sales period</h3></div><p className="mt-1 text-xs text-slate-500">When sales are highest in the selected {modeLabel.toLowerCase()} period</p></div><div className="rounded-2xl border border-sky-400/20 bg-sky-400/10 px-4 py-3"><p className="text-[10px] uppercase tracking-widest text-slate-400">Highest revenue</p><p className="mt-1 text-lg font-semibold text-sky-300">{peakPeriod.label}</p><p className="text-xs text-slate-400">{peakPeriod.time} · {money(peakPeriod.revenue)}</p></div></div><div className="mt-4 grid gap-2 sm:grid-cols-2 lg:grid-cols-3">{periodStats.map((period) => <div key={period.key} className={`rounded-2xl border p-3 ${period.key === peakPeriod.key ? 'border-sky-400/40 bg-sky-400/10' : 'border-white/5 bg-white/[.025]'}`}><div className="flex items-center justify-between gap-2"><div><p className="font-medium">{period.label}</p><p className="text-[11px] text-slate-500">{period.time}</p></div>{period.key === peakPeriod.key && <span className="rounded-full bg-sky-400 px-2 py-1 text-[9px] font-bold text-slate-950">PEAK</span>}</div><div className="mt-3 flex items-end justify-between"><div><p className="text-lg font-semibold">{money(period.revenue)}</p><p className="text-[11px] text-slate-500">{period.transactions} transactions</p></div><p className="text-xs text-sky-300">Profit {money(period.profit)}</p></div></div>)}</div></div><div className="mt-4 glass rounded-3xl p-5"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h3 className="font-semibold">Share analytics</h3><p className="mt-1 text-xs text-slate-500">Generate a formatted PDF for the selected report and share it through WhatsApp or another app.</p></div><button onClick={sharePdf} className="inline-flex items-center justify-center gap-2 rounded-2xl bg-sky-400 px-4 py-3 text-sm font-semibold text-slate-950"><Share2 size={17}/> Share PDF</button></div></div><div className="mt-4 grid gap-4 lg:grid-cols-[1.2fr_.8fr]"><div className="glass rounded-3xl p-5"><div className="flex items-start justify-between gap-3"><div><h3 className="font-semibold">Revenue trend</h3><p className="mt-1 text-xs text-slate-500">{modeLabel} · {rangeDays} day{rangeDays === 1 ? '' : 's'}</p></div><span className="rounded-full bg-sky-400/10 px-3 py-1 text-[10px] font-semibold text-sky-300">{money(revenue)}</span></div><div className="mt-4 h-72">{trend.length ? <ResponsiveContainer width="100%" height="100%"><BarChart data={trend}><CartesianGrid strokeDasharray="3 3" stroke="rgba(255,255,255,.06)"/><XAxis dataKey="label" stroke="#64748b" fontSize={10}/><YAxis stroke="#64748b" fontSize={10}/><Tooltip contentStyle={{ background: '#07101d', border: '1px solid rgba(255,255,255,.1)' }} formatter={(value) => money(Number(value))}/><Bar dataKey="revenue" fill="#38bdf8" radius={[6, 6, 0, 0]}/></BarChart></ResponsiveContainer> : <div className="grid h-full place-items-center text-sm text-slate-500">No sales in this period.</div>}</div></div><div className="glass rounded-3xl p-5"><h3 className="font-semibold">Top selling items</h3><div className="mt-4 h-72">{top.length ? <ResponsiveContainer width="100%" height="100%"><BarChart data={top} layout="vertical" margin={{ left: 8, right: 12 }}><CartesianGrid strokeDasharray="3 3" stroke="rgba(255,255,255,.06)"/><XAxis type="number" stroke="#64748b" fontSize={10}/><YAxis type="category" dataKey="name" width={90} stroke="#64748b" fontSize={10}/><Tooltip contentStyle={{ background: '#07101d', border: '1px solid rgba(255,255,255,.1)' }}/><Bar dataKey="count" fill="#38bdf8" radius={[0, 6, 6, 0]}/></BarChart></ResponsiveContainer> : <div className="grid h-full place-items-center text-sm text-slate-500">No product sales in this period.</div>}</div></div></div><div className="glass mt-4 rounded-3xl p-5"><h3 className="font-semibold">Highest profit yield</h3><div className="mt-4 grid gap-2 md:grid-cols-2 lg:grid-cols-3">{[...top].sort((a, b) => b.profit - a.profit).map((x, i) => <div key={x.name} className="flex items-center justify-between rounded-2xl bg-white/[.03] p-3"><div><p className="font-medium">{i + 1}. {x.name}</p><p className="text-xs text-slate-500">{x.count} units</p></div><span className="font-semibold text-sky-300">{money(x.profit)}</span></div>)}{!top.length && <p className="text-sm text-slate-500">No profit data for this period.</p>}</div></div></section>;
}

function Metric({ label, value }: { label: string; value: string }) { return <div className="glass glow rounded-3xl p-5"><p className="text-xs uppercase tracking-widest text-slate-500">{label}</p><p className="mt-2 text-3xl font-semibold">{value}</p></div>; }

function MenuManager({ products, setProducts, editing, setEditing }: { products: Product[]; setProducts: React.Dispatch<React.SetStateAction<Product[]>>; editing: Product | null; setEditing: (p: Product | null) => void }) { const [draft, setDraft] = useState<Product>(editing ?? { id: '', name: '', price: 0, profit_margin: 0, image_url: '', daily_count: 0 }); const save = () => { if (!draft.name || !draft.price) return; if (draft.id) setProducts((ps) => ps.map((x) => x.id === draft.id ? { ...draft, daily_count: x.daily_count } : x)); else setProducts((ps) => [...ps, { ...draft, id: crypto.randomUUID(), daily_count: 0 }]); setEditing(null); setDraft({ id: '', name: '', price: 0, profit_margin: 0, image_url: '', daily_count: 0 }); }; return <section><div className="mb-5 flex items-center justify-between"><div><p className="text-xs uppercase tracking-widest text-slate-500">Admin inventory</p><h2 className="mt-1 text-2xl font-semibold">Menu Manager</h2></div><Settings2 className="text-sky-300"/></div><div className="grid gap-4 lg:grid-cols-[.9fr_1.1fr]"><div className="glass rounded-3xl p-5"><h3 className="font-semibold">{editing ? 'Edit product' : 'Add product'}</h3><div className="mt-4 space-y-3"><input value={draft.name} onChange={(e) => setDraft({ ...draft, name: e.target.value })} placeholder="Product name" className="w-full rounded-2xl border border-white/10 bg-white/[.04] px-4 py-3"/><div className="grid grid-cols-2 gap-3"><input type="number" value={draft.price || ''} onChange={(e) => setDraft({ ...draft, price: Number(e.target.value) })} placeholder="Selling price" className="w-full rounded-2xl border border-white/10 bg-white/[.04] px-4 py-3"/><input type="number" value={draft.profit_margin || ''} onChange={(e) => setDraft({ ...draft, profit_margin: Number(e.target.value) })} placeholder="Profit" className="w-full rounded-2xl border border-white/10 bg-white/[.04] px-4 py-3"/></div><input value={draft.image_url ?? ''} onChange={(e) => setDraft({ ...draft, image_url: e.target.value })} placeholder="Image URL" className="w-full rounded-2xl border border-white/10 bg-white/[.04] px-4 py-3"/><button onClick={save} className="w-full rounded-2xl bg-sky-400 py-3 font-semibold text-slate-950">{editing ? 'Save changes' : 'Create product'}</button>{editing && <button onClick={() => { setEditing(null); setDraft({ id: '', name: '', price: 0, profit_margin: 0, image_url: '', daily_count: 0 }); }} className="w-full rounded-2xl border border-white/10 py-3 text-sm text-slate-300">Cancel</button>}</div></div><div className="space-y-2">{products.map((p) => <div key={p.id} className="glass flex items-center gap-3 rounded-2xl p-3"><div className="h-14 w-14 overflow-hidden rounded-xl bg-slate-900">{p.image_url ? <img src={p.image_url} alt="" className="h-full w-full object-cover"/> : <div className="grid h-full place-items-center text-[10px] text-slate-600">No image</div>}</div><div className="min-w-0 flex-1"><p className="truncate font-medium">{p.name}</p><p className="text-xs text-slate-500">{money(p.price)} · Profit {money(p.profit_margin)}</p></div><button onClick={() => { setEditing(p); setDraft(p); }} className="rounded-xl border border-white/10 p-2 text-slate-300"><Settings2 size={16}/></button><button onClick={() => setProducts((ps) => ps.filter((x) => x.id !== p.id))} className="rounded-xl border border-red-400/20 p-2 text-red-300"><Trash2 size={16}/></button></div>)}{!products.length && <div className="glass rounded-3xl p-8 text-center text-slate-500">No products yet.</div>}</div></div></section>; }
