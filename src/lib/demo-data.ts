import { Product, Sale } from './types';

// Deliberately empty: production data comes from the connected store database.
export const demoProducts: Product[] = [];
export const demoSales: Sale[] = [];
